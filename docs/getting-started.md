# Getting Started

Run ClawDesk locally:

```bash
cd ~/Desktop/openclaw-mission-control
cd frontend && npm install
cd ..
./start.sh
```

Frontend: http://localhost:5173

Backend: http://localhost:3001

Agents connect with a hostname or IP address, a pasted/uploaded SSH private key, and the OpenClaw gateway token from the remote host.
